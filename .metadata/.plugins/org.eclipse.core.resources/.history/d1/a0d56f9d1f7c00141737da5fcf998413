package domain;

import java.util.*;

public class ProductCatalog {
  private Hashtable<Integer, ProductSpecification> productSpecifications = new Hashtable<Integer, ProductSpecification>();

 

  public ProductSpecification specification(int upc) {
    return productSpecifications.get(new Integer(upc));
  }
}
